---

# remote path of the config
config_path: /conf/config.xml
config_path_packer: /home/hetzner/packer/templates/opnsense/config.xml

# local path for the config
# local_config_path: /tmp/config-{{ inventory_hostname }}.xml

local_config_path: /home/hetzner/ansible/roles/ansible-opnsense/templates/config.xml

remote: false